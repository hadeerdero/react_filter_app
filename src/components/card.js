const Cart = (props) => {
    return (
        <div>
            <img></img>
            <p>
                <h1>{props.header}</h1>
                <div>{props.title}</div>
            </p>
        </div>
    )
}

export default Cart